#include <stdio.h>
#include <conio.h>

int main(int argc)
{
    int a, b, c;

    scanf("%d%d", &a, &b);

    c = a + b;

    printf(" the value of a in %d, value of b is %d, and value of c is %d", a, b, c);

    return 0;
}